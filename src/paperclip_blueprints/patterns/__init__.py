"""Use-case pattern seeds (v0.1b, US2).

A pattern seeds the org_planner with a suggested org shape that the prompts then
customize against the brief. The registry and ``OrgSeed`` are filled in during
US2; this package exists so foundational and US1 work can import the namespace.
"""
